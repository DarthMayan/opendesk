# Bitácora — 2026-05-18

## Equipo
Diego Morales Gómez · Diego Salvador Padilla Victoria · Joseph Emiliano Arias Limas

## Decisiones tomadas
- Joseph adelanta el frontend de auth y tickets en paralelo al backend, sobre el
  scaffold ya mergeado a `main`.
- Las plantillas extienden `base.html` y se montan con Bootstrap 5 (CDN, sin build
  de Node), consistente con el stack acordado el 17 may.
- El blueprint de auth queda con rutas `/login`, `/register`, `/logout`
  (sin `url_prefix`), para que el navbar quede limpio.

## Avances
- Rama `feature/ui-auth-tickets` abierta desde `main`.
- Plantillas Jinja: `auth/login.html`, `auth/register.html`,
  `tickets/list.html`, `tickets/detail.html`.
- `base.html` actualizado con navbar dinámica (muestra usuario y "Cerrar sesión"
  cuando hay sesión) y bloque de mensajes flash.
- Blueprint `app/auth/routes.py` con login/register/logout funcional usando
  Flask-Login y `werkzeug.security` para el hash.
- Blueprint `app/tickets/routes.py` (solo lectura por ahora) para que la UI
  pueda renderizar lista y detalle.
- `tests/conftest.py` con fixtures de app y cliente; `pytest.ini` agregado;
  `tests/test_smoke.py` ampliado.

## Obstáculos
- Hay solape de responsabilidades: el backend de auth lo está adelantando Joseph
  (área de Diego Salvador). Acordado revisarlo en code review cuando Salvador
  se incorpore.

## Siguiente
- Code review de la rama por algún integrante antes de mergear.
- Iniciar UI del dashboard de métricas (M4) sobre la misma rama o una nueva.
