# Bitácora — 2026-05-21

## Equipo
Diego Morales Gómez · Diego Salvador Padilla Victoria · Joseph Emiliano Arias Limas

## Decisiones tomadas
- Consolidar en `main` los avances de UI antes del corte de E1+E2 (22 may).
- Mantener el patrón de ramas por tipo: `feature/*`, `chore/*`, `docs/*`.
- Para el race condition de Docker se elige el fix limpio (healthcheck de Postgres + `condition: service_healthy`) en lugar de un script de espera ad-hoc.

## Avances
- Merge a `main` de `feature/ui-dashboard`, que incluye los commits previos de
  `feature/ui-auth-tickets` (UI de login/registro, navbar con sesión, lista y detalle
  de tickets, dashboard de métricas) y un borrador del manual de instalación.
- Suite de pruebas en verde tras el merge: **9/9 pytest passed**.
- Validación de `docker-compose up`: levanta correctamente y responde HTTP 200 en
  `/`, `/health` y `/login` en `http://localhost:8000` (con el fix de la rama
  `chore/docker`).
- Actualización del [CLAUDE.md](../../CLAUDE.md): cronograma con estado por fase,
  lista detallada de tareas por integrante con marcas de avance, conteo de
  commits y bitácora interna.

## Obstáculos
- **Race condition en `docker-compose.yml`**: el contenedor `web` arrancaba
  antes de que Postgres aceptara conexiones y moría con `connection refused`.
  Fix abierto en la rama `chore/docker` (healthcheck con `pg_isready` + `depends_on`
  con `condition: service_healthy`); validado: la primera corrida tras `docker compose
  down -v` ya inicia limpia, sin restart manual.
- Distribución desigual de commits: Diego Salvador todavía sin contribuciones al
  repo. R2 exige ≥30 commits distribuidos; se le notifica para arrancar
  `feature/auth` (decorador `role_required`) o `feature/tickets-backend`.

## Siguiente
- Diego Salvador: arrancar el backend de roles (M1) y el CRUD de tickets (M2).
- Ampliar la suite pytest con casos específicos de auth/roles y tickets.
- Preparar borradores de E1 (documento 3–5 pág) y E2 (presentación) para el 22 may.
- Limpiar ramas ya mergeadas en GitHub (`feature/ui-auth-tickets`,
  `feature/ui-dashboard`, `feature/scaffold-inicial`).

## Cierre del día (extras tras la primera versión de esta bitácora)
- `chore/docker` y `docs/bitacora` mergeadas a `main` sin conflictos.
- Bitácoras retroactivas del 18 y 20 may agregadas en una nueva ronda de
  `docs/bitacora` y mergeadas a `main`.
- **Despliegue Render RD1 LIVE** en https://opendesk-a7uf.onrender.com
  (Blueprint con `render.yaml`, sync del commit `7760e16`). Endpoints `/`,
  `/health` y `/login` responden HTTP 200 desde producción.
- Tablero GitHub configurado: 3 milestones (E1+E2 22 may · F3 24 may ·
  E3+E4 26 may) y 12 issues con cuerpo completo; #7 (RD1) cerrado con
  evidencia del deploy.
- Conteo de commits en `main` al cierre: 10 de Diego Morales · 4 de Joseph
  Arias · 0 de Diego Salvador (riesgo abierto, ya notificado).
- Puntos extra asegurados hoy: **RD1 (+10) y RD4 (+5) = +15 / +30**.